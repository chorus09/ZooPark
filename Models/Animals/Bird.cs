namespace Module18.Animals;
public class Bird : Animal {
    public Bird(int id, string name, int age, double weight, double health, string status) :
        base(id, name, age, weight, health, status) { }
}
